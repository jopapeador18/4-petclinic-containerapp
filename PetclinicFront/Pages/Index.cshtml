@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Bienvenido a Petclinic</h1>
    <p>Aprender más sobre <a href="https://docs.microsoft.com/aspnet/core">Construir sitios web con ASP.NET Core</a>.</p>
    <img src="~/images/pets.png" alt="Pets" />

    @*<h1>Variables de entorno encontradas:</h1>
    @foreach (var item in Model.env)
    {
        <p><strong>@item.Key</strong>: @item.Value </p>
    }*@
</div>